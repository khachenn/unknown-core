# Group depending on upstream's counterpart to make it easier to manage
# dependencies in brave/ (//chrome/utility does not depend on //unknown/utility,
# so //unknown/utility becomes essentially an alias for //chrome/utility now).
group("utility") {
  public_deps = [ "//chrome/utility" ]
}
